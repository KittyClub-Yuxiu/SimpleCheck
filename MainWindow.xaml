<Window x:Class="SimpleCheck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleCheck"
        xmlns:vm="clr-namespace:SimpleCheck.ViewModels"
        xmlns:models="clr-namespace:SimpleCheck.Models"
        xmlns:converters="clr-namespace:SimpleCheck.Converters"
        mc:Ignorable="d"
        Title="Simple Check" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:TaskTypeConverter x:Key="TaskTypeConverter"/>
        <local:BoolToTextDecorationConverter x:Key="BoolToTextDecorationConverter"/>
        <converters:TaskTimeStatusConverter x:Key="TaskTimeStatusConverter"/>
        <CubicEase x:Key="CubicEase" EasingMode="EaseOut"/>
        
        <SolidColorBrush x:Key="WindowBackground" Color="#FDFDF9" />
        
        <SolidColorBrush x:Key="AccentColor" Color="#5A8B7E" />
        
        <SolidColorBrush x:Key="PrimaryColor" Color="#7EB5A6" />
        
        <SolidColorBrush x:Key="TextColor" Color="#333333" />
        <SolidColorBrush x:Key="TextLightColor" Color="#888888" />
        
        <SolidColorBrush x:Key="BorderColor" Color="#EFEFEF" />

        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#10000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border Background="{StaticResource WindowBackground}" CornerRadius="16" Margin="15" Padding="20"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.2" Color="#334433"/>
        </Border.Effect>
       
        <Grid x:Name="MainContentGrid" Opacity="0">
            <Grid.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="MainContentGrid" 
                                             Storyboard.TargetProperty="Opacity" 
                                             From="0" To="1" Duration="0:0:0.8" 
                                             EasingFunction="{StaticResource CubicEase}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Grid.Triggers>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Simple Check" FontSize="20" FontWeight="Bold" 
                               Foreground="{StaticResource TextColor}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" Content="ℹ" Style="{StaticResource TitleButtonStyle}" 
                        Command="{Binding OpenAboutCommand}"
                        Foreground="{StaticResource TextLightColor}" Margin="0,0,5,0" ToolTip="关于"/>
                
                <Button Grid.Column="2" Content="⚙" Style="{StaticResource TitleButtonStyle}" 
                        Command="{Binding OpenSettingsCommand}"
                        Foreground="{StaticResource TextLightColor}" Margin="0,0,5,0" ToolTip="设置"/>

                <Button Grid.Column="3" Content="一" Style="{StaticResource TitleButtonStyle}" 
                        Click="Minimize_Click" Foreground="{StaticResource TextLightColor}" Margin="0,0,5,0"/>
                
                <Button Grid.Column="4" Content="✕" Style="{StaticResource TitleButtonStyle}"
                        Click="Close_Click" Foreground="#FF6B6B" FontWeight="Bold"/>
            </Grid>

            <TextBlock Grid.Row="1" Text="{Binding DailyQuote}" 
                       FontSize="13" FontStyle="Italic" Foreground="{StaticResource TextLightColor}" 
                       TextWrapping="Wrap" Margin="0,0,0,20" HorizontalAlignment="Left" Opacity="0.8"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                <ItemsControl ItemsSource="{Binding TodoItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Name="ItemBorder" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" 
                                    Padding="10,12" Background="Transparent" CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <CheckBox IsChecked="{Binding IsCompleted}" 
                                              Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                              CommandParameter="{Binding}"
                                              VerticalAlignment="Top" Margin="0,3,12,0"
                                              Cursor="Hand">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                    
                                    <TextBlock Grid.Column="1" Text="{Binding Content}" 
                                               VerticalAlignment="Top" FontSize="15"
                                               TextWrapping="Wrap" Margin="0,0,10,0" LineHeight="22">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                        <Setter Property="Foreground" Value="#AAAAAA"/>
                                                        <Setter Property="Opacity" Value="0.7"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    
                                    <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                        
                                        <Border Background="{StaticResource AccentColor}" CornerRadius="6" Padding="8,2" Margin="0,0,8,0">
                                            <TextBlock FontSize="11" Foreground="White" FontWeight="SemiBold">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource TaskTimeStatusConverter}">
                                                        <Binding Path="StartTime"/>
                                                        <Binding Path="EndTime"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </Border>

                                        <Border Background="#F0F0F0" CornerRadius="6" Padding="8,2">
                                            <TextBlock Text="{Binding Type, Converter={StaticResource TaskTypeConverter}}" 
                                                       FontSize="11" Foreground="{StaticResource TextLightColor}"/>
                                        </Border>
                                    </StackPanel>

                                    <Button Grid.Column="3" Grid.RowSpan="2" Content="✕" 
                                            Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            Width="28" Height="28" VerticalAlignment="Center"
                                            Background="Transparent" BorderThickness="0" 
                                            Foreground="#DDDDDD" 
                                            Cursor="Hand" ToolTip="删除此日程"
                                            Name="DeleteBtn" Visibility="Hidden">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent">
                                                    <TextBlock Text="✕" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F8FDFB"/> </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                
                                </Border>
                            <DataTemplate.Triggers>
                                <Trigger SourceName="ItemBorder" Property="IsMouseOver" Value="True">
                                    <Setter TargetName="DeleteBtn" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="DeleteBtn" Property="Foreground" Value="#FF8888"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Button Grid.Row="3" Command="{Binding OpenTaskEditorCommand}"
                    Width="56" Height="56" Margin="0,0,10,15"
                    HorizontalAlignment="Right"
                    Background="{StaticResource AccentColor}" Foreground="White" BorderThickness="0"
                    Cursor="Hand" ToolTip="添加新日程"
                    RenderTransformOrigin="0.5, 0.5">
                <Button.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Button.RenderTransform>

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border CornerRadius="28" Background="{TemplateBinding Background}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.3" Color="#5A8B7E"/>
                                </Border.Effect>
                                <TextBlock Text="+" FontSize="30" FontWeight="Light" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           Margin="0,0,0,4"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>